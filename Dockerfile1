FROM golang:1.18.3-alpine

WORKDIR /app

RUN apk add --no-cache git

COPY go.mod .
COPY go.sum .

RUN go mod download

COPY . .

RUN go build -o server cmd/gophermart/main.go

EXPOSE 8000

ENV RUN_ADDRESS=:8000
ENV ACCRUAL_SYSTEM_ADDRESS=:8080
ENV DATABASE_URI=123

CMD ["./server"]